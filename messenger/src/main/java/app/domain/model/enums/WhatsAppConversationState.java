package app.domain.model.enums;

/**
 * Representa los estados posibles de una conversación en el bot de WhatsApp.
 */
public enum WhatsAppConversationState {
    AWAITING_PIN,
    AWAITING_PLATE,
    MENU
}
