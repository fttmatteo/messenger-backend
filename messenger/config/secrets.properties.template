# ============================================
# SECRETS - NO HACER COMMIT DE ESTE ARCHIVO
# ============================================
# Este archivo contiene secretos sensibles y NO debe subirse a Git
# Está protegido en .gitignore

# JWT Secret Key (Base64)
# Genera una nueva clave con: openssl rand -base64 64
jwt.secret=REEMPLAZA_ESTA_CLAVE_CON_UNA_GENERADA_POR_OPENSSL

# Instrucciones:
# 1. Genera una clave segura: openssl rand -base64 64
# 2. Reemplaza el valor de jwt.secret con la clave generada
# 3. NUNCA hagas commit de este archivo con la clave real
